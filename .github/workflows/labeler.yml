---
name: Pull Request Labels

on:
  pull_request_target:
    types:
      - opened
      - reopened
      - synchronize
  workflow_dispatch:

jobs:
  call:
    uses: chrysa/github-actions/.github/workflows/labeler.yml@v1.2.3
